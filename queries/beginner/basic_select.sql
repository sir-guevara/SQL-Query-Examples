-- Basic SELECT Query

-- Description:
-- Retrieve the `first_name` and `last_name` columns from an `employees` table.

-- Dataset:
-- File: employees.csv
-- Columns: `first_name`, `last_name`

SELECT first_name, last_name
FROM employees;
